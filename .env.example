# Copy this file to `.env` (and never commit the real .env) or export these
# as real environment variables before running `python main.py`.

FLASK_SECRET_KEY=change-me-to-a-random-string

# --- Contact Us email ---
# Gmail example: create an "App Password" at
# https://myaccount.google.com/apppasswords (requires 2-Step Verification).
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=asiyamohsima@gmail.com
SMTP_PASSWORD=mpld eymk cmre ppew
CONTACT_RECEIVER_EMAIL=asiyamohsima@gmail.com
